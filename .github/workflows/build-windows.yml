name: Build MyVault

# Everything here fits inside GitHub's free allowance for a private repository
# (2,000 runner minutes and 500 MB of artifact storage per month).
#
#   • Tests run on every push. They run on Linux, the cheapest runner, and take
#     a couple of minutes — a few hundred pushes a month would still fit.
#
#   • The Windows installer is BUILT on every push, so a break in the packaging
#     — the NSIS script, the icon, the licence encoding — is caught here rather
#     than the first time you try to hand the app to a shop. Runner minutes are
#     the plentiful allowance.
#
#   • The Linux AppImage is built on every push too, and then STARTED: the job
#     unpacks the file it just made, opens the real window, saves a product and
#     reopens it. "The build succeeded" and "the program works" are different
#     claims, and only one of them is worth making to a shop.
#
#   • The installers are only KEPT when you ask for them: press "Run workflow"
#     on this page, or push a version tag. Storage is the scarce allowance —
#     each build is about 175 MB against 500 MB — so an ordinary push verifies
#     the build and then throws it away.
#
#   • Kept installers expire after 7 days. Download the one you want and hand it
#     out; it does not need to sit here.
#
#   • For a version you actually want to keep, fill in "Release tag" when you
#     press "Run workflow" (or push a v… tag). That publishes a Release with the
#     two .exe files attached: a permanent, one-click download that does not
#     expire and does not count against the 500 MB artifact allowance.
#
# You can also skip all of this and build on your own machine for nothing:
#   npm install && npm run dist:win     (on Windows)
#   npm install && npm run dist:linux   (on Linux)

on:
  push:
    branches: ['**']
    tags: ['v*']
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag, e.g. v1.0.0 — leave blank to just build and keep the files for 7 days'
        required: false
        default: ''

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test

      # Starts the real application five times over against one data folder and
      # checks the stock is still there each time. It needs the interface built,
      # because it clicks the actual buttons; the display a CI runner does not
      # have is provided by the xvfb inside the script itself.
      - run: npm run build
      - name: Does it remember the stock across restarts?
        run: npm run test:e2e

  build:
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build the Windows installer and portable exe
        run: npm run dist:win

      # NSIS prints a warning for every installer string a language has no
      # translation for and falls back to English on. They are cosmetic, so
      # warningsAsErrors is off — but that means the log is the only place a
      # genuine packaging warning would show up. Worth a glance when it changes.
      - name: Confirm both installers were produced
        shell: pwsh
        run: |
          $exes = Get-ChildItem release -Filter *.exe
          $exes | ForEach-Object { "{0}  {1:N0} MB" -f $_.Name, ($_.Length / 1MB) }
          if ($exes.Count -lt 2) { throw "expected an installer and a portable exe, found $($exes.Count)" }

      # Kept only for a tag or a manual run — see the note at the top about
      # storage. Download these from the run's summary page, then hand the .exe
      # to a shop however you like: USB stick, email, WeTransfer.
      - name: Installer
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: MyVault-windows-installer
          # latest.yml is the small file the in-app updater reads to learn what
          # the newest version is and what its installer should hash to. Without
          # it beside the .exe, "Check now" finds nothing.
          path: |
            release/*setup*.exe
            release/latest.yml
          if-no-files-found: error
          retention-days: 7

      - name: Portable build
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: MyVault-windows-portable
          path: release/*portable*.exe
          if-no-files-found: error
          retention-days: 7

  # The Linux build, and then the part that matters: does the file run?
  #
  # An AppImage is the one Linux format that needs no installing and no root —
  # download, make it executable, double-click. The .deb is there for anyone on
  # Debian or Ubuntu who would rather it appeared in their menu.
  build-linux:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build the AppImage and the .deb
        run: npm run dist:linux

      # The check a "build succeeded" line cannot make. It unpacks the AppImage
      # that was just produced, starts it, puts a product in through the real
      # bridge, closes it and opens it again to see whether the stock came back.
      - name: Does the packaged app actually run?
        run: npm run test:packaged

      - name: Confirm both Linux files were produced
        run: |
          set -euo pipefail
          ls -lh release/*.AppImage release/*.deb
          test -f release/latest-linux.yml

      - name: Linux build
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: MyVault-linux
          path: |
            release/*.AppImage
            release/*.deb
            release/latest-linux.yml
          if-no-files-found: error
          retention-days: 7

  # Turns a build into something anybody can download in one click, months from
  # now, from the repository's Releases page — no zip to unpack, no expiry date.
  # The repository is public, so this is also what the in-app updater reads:
  # a shop needs no account and no token to reach it.
  release:
    needs: [build, build-linux]
    if: startsWith(github.ref, 'refs/tags/v') || inputs.release_tag != ''
    runs-on: ubuntu-latest
    permissions:
      # Needed to create the tag and publish the release. Nothing else is granted.
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Collect the installers and the Linux build
        uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true

      - name: Publish the release
        env:
          GH_TOKEN: ${{ github.token }}
          # A tag push names itself; a manual run uses the tag you typed in.
          TAG: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || inputs.release_tag }}
        run: |
          set -euo pipefail
          ls -l release

          cat > notes.md <<'NOTES'
          MyVault — the offline stock book for small shops.

          ### Windows

          **MyVault-…-setup.exe** — the normal installer. Run it, choose your
          language, and MyVault appears in the Start menu. Windows SmartScreen will
          say the publisher is unknown, because the file is not code-signed: choose
          **More info → Run anyway**.

          **MyVault-…-portable.exe** — nothing to install. Runs from the folder it
          sits in (a USB stick is fine) and keeps its data beside itself.

          ### Linux

          **MyVault-…-linux-x86_64.AppImage** — nothing to install. Download it, then:

          ```
          chmod +x MyVault-*.AppImage
          ./MyVault-*.AppImage
          ```

          If your system has no FUSE, run `./MyVault-*.AppImage --appimage-extract-and-run`
          instead. On a headless or minimal system add `--no-sandbox`.

          **MyVault-…-linux-amd64.deb** — for Debian or Ubuntu, if you would rather it
          appeared in your applications menu: `sudo apt install ./MyVault-*.deb`

          Everything stays on the computer. The app has no internet access at all.

          © Aristotelis Katsigiannis. Free to use — see LICENSE.
          NOTES

          # latest.yml is the small file the in-app updater reads to learn the
          # newest version and the checksum its installer must match. Without it
          # beside the .exe, "Check now" finds nothing.
          test -f release/latest.yml || { echo "::error::latest.yml is missing"; exit 1; }

          # Re-running the same tag replaces the files rather than failing.
          # Windows first, because that is what a shop runs; the Linux files are
          # there for testing and for anyone who wants them.
          FILES=(release/*.exe release/latest.yml release/*.AppImage release/*.deb)
          test -f release/latest-linux.yml && FILES+=(release/latest-linux.yml)

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${FILES[@]}" --clobber
          else
            gh release create "$TAG" "${FILES[@]}" \
              --target "$GITHUB_SHA" \
              --title "MyVault $TAG" \
              --notes-file notes.md
          fi

          gh release view "$TAG" --json tagName,assets \
            --jq '"published \(.tagName): " + ([.assets[].name] | join(", "))'
